build/
build-debug/
out/
.vs/
*.user

# What a failing golden test drew, written next to the baseline it disagrees
# with so the two can be opened side by side.  Diagnostic, never committed.
*.actual.png
build-asan/

# Editor/assistant configuration.  Local tooling preferences, not part of the
# library -- a contributor's setup is their own business, and the public
# history stays about the code.
.claude/
